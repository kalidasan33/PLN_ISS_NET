@(Html.Kendo().DropDownListFor(m => m)
 .DataSource(source =>
  {
      source.Read(read =>
      {
          read.Action("GetSkuSizes", "Order").Type(HttpVerbs.Post).Data("WO.retrieSizeData");
      }).ServerFiltering(false);
  })
      .DataValueField("Size")
      .DataTextField("SizeDesc")
  .Events(e =>
  {
      e.Change("WO.onSizeChanged");
  })
)

