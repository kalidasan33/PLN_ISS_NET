@(Html.Kendo().DropDownListFor(m => m)
 .DataSource(source =>
  {
      source.Read(read =>
      {
          read.Action("GetSkuSizes", "Order").Type(HttpVerbs.Post).Data("retrieveSizeData");
      }).ServerFiltering(false);
  })
      .DataValueField("Size")
      .DataTextField("SizeDesc")
  .Events(e =>
  {
    e.Select("onSizeSelect");
  })
)
