@(Html.Kendo().DropDownListFor(m => m)
 .DataSource(source =>
  {
      source.Read(read =>
      {
          read.Action("GetSkuColors", "Order").Type(HttpVerbs.Post).Data("retrieveColorData");
      }).ServerFiltering(false);
  })
  .DataValueField("Color")
  .DataTextField("ColorName")
  .Events(e =>
  {
    e.Select("onColorSelect");
  })
)


