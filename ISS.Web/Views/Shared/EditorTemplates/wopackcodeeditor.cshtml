@(Html.Kendo().DropDownListFor(m => m)
 .DataSource(source =>
  {
      source.Read(read =>
      {
          read.Action("WOMGetPackCode", "Order").Type(HttpVerbs.Post);
      }).ServerFiltering(false);
  })
      .DataValueField("PackCode")
      .DataTextField("PackCode")
)
